import { Meta, Story } from '@storybook/addon-docs';

import { Box } from '@real-system/box';

import { makeMdxParameters } from '/.storybook/utils';

{/* * Meta requires a component -- just using Box so this story renders */}

<Meta
  title="Sandbox/Live Coding"
  component={Box}
  parameters={{
    ...makeMdxParameters({
      playroom: {
        disable: false,
        code: '<real.div fontWeight={3} fontSize={2}>A Real Element Primitive</real.div>',
      },
      previewTabs: {
        canvas: { hidden: true },
        'storybook/playroom/panel': {
          hidden: false,
        },
      },
    }),
  }}
/>

<Story name="Playroom" />

## Real System Component Sandbox

Click on the [playroom](?path=/playroom/sandbox-live-coding--playroom) tab in the top-left of the toolbar
